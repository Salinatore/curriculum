# .github/workflows/build-cv.yml
name: Build and Deploy CV

on:
  push:
    branches: [main]
    paths:
      - '*.yaml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b 
        with: 
            version: "0.11.26"

      - name: Render CV to PDF
        run: |
          uvx --from 'rendercv[full]' rendercv render Alessandro_Gardini_CV_IT.yaml \
            --dont-generate-markdown --dont-generate-html --dont-generate-png

      - name: Prepare Pages folder
        run: |
          mkdir -p public
          cp rendercv_output/*.pdf public/cv.pdf

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v7